from django.apps import AppConfig


class MainAppConfig(AppConfig):
    """Configuration class for the 'main_app' app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'main_app'
